# Garmin Connect Credentials
GARMIN_EMAIL=your-email@example.com
GARMIN_PASSWORD=your-password

# MCP Server Configuration
# IMPORTANT: Set to 'streamable-http' to enable HTTP server mode (required for nginx)
# Options: stdio (default), tcp, streamable-http
GARMIN_MCP_TRANSPORT=streamable-http

# Host binding (0.0.0.0 allows access from nginx in Docker network)
GARMIN_MCP_HOST=0.0.0.0

# Internal container port (should match Dockerfile EXPOSE)
GARMIN_MCP_PORT=8000

# External host port mapping (if accessing from outside Docker network)
GARMIN_HOST_PORT=9000

# Bind address on host (0.0.0.0 allows external access, 127.0.0.1 for localhost only)
GARMIN_MCP_BIND=0.0.0.0

# Optional: Use uv for faster execution
GARMIN_USE_UV=false
